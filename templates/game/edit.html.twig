{% extends 'base.html.twig' %}

{% block title %}Modifier {{ game.name }}{% endblock %}

{% block body %}
    <h1>Modifier {{ game.name }}</h1>

    {{ include('game/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('game_index') }}" class="btn btn-outline-light">Retour liste des jeux</a>

    {{ include('game/_delete_form.html.twig') }}
{% endblock %}
