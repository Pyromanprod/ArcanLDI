{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.video) }}
    {{ form_errors(form) }}
    <button type="submit" class="btn btn-outline-primary">Envoyer</button>
    {{ form_end(form) }}
{% endblock %}

{% block title %}Ajouter Album video{% endblock %}


