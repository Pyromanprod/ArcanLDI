{{ form_start(form) }}
    {{ form_row(form.title, {
        label: 'Titre',
        attr: {
            placeholder: '',
            class: 'bg-dark border-0 border-bottom border-ivoire rounded-0 text-light'
        },
        row_attr: {
            class: 'form-floating bg-dark text-ivoire mb-3'
        }
    }) }}
    {{ form_row(form.content, {
        label: false,
        attr: {
            placeholder: '',
            class: 'bg-dark border-0 border-bottom border-ivoire rounded-0 text-light'
        },
        row_attr: {
            class: 'form-floating bg-dark text-ivoire mb-3'
        }
    }) }}
    {{ form_row(form.roleGroupe, {
        label: 'Groupe',
        attr: {
            placeholder: '',
            class: 'bg-dark border-0 border-bottom border-ivoire rounded-0 text-light'
        },
        row_attr: {
            class: 'form-floating bg-dark text-ivoire mb-3'
        }
    }) }}
    <button class="btn {{ button_color|default('btn-outline-success') }}">{{ button_label|default('Créer') }}</button>
<a class="btn btn-outline-light" href="{{ path('article_index') }}">Annuler</a>
{{ form_end(form) }}
