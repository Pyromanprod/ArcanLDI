{% extends 'base.html.twig' %}

{% block title %}Ajouter des joueurs{% endblock %}

{% block body %}
    <div class="container">
    <h1 class="text-center">Ajouter des joueurs au rôle {{ role }}</h1>
        <div class="row">
            <div class="text-center my-3" id="searchmember">
                <label for="search">Recherche</label>
                <input type="text" id="search" class="search form-text">
            </div>
            <div class="col-12 col-md-8 mx-auto">
                {{ form_start(form) }}
                {{ form_widget(form.name,{
                    label_attr: {
                        class : 'searching'
                    }
                }) }}
                <button class="btn btn-outline-success" type="submit">Ajouter</button>
                <a class="btn btn-outline-warning" href="{{ path('role_groupe_index') }}">Annuler</a>
                {{ form_end(form) }}
            </div>
        </div>
    </div>


{% endblock %}
    {% block script %}
        <script src="{{ asset('js/search.js') }}"></script>
    {% endblock %}
