<form method="post" action="{{ path('role_groupe_delete', {'id': role_groupe.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer ce role?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ role_groupe.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
