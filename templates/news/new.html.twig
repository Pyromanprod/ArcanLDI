{% extends 'base.html.twig' %}

{% block title %}Créer une actualité{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <h1 class="text-center">Créer une actualité</h1>
            <div class="text-center">

                <a class="btn-outline-light btn" href="{{ path('news_index') }}">Retour à la liste des actualités</a>
                <a class="btn-outline-light btn" href="{{ path('upload_photo_pour_ckeditor') }}" target="_blank">liste des photos sur le serveur</a>
            </div>
            <div class="col-12 col-md-8 mx-auto">
                {{ include('news/_form.html.twig') }}

            </div>
        </div>
    </div>


{% endblock %}
